name: Build Samsung Galaxy A04 Kernel with Wild_KSU

on:
  workflow_dispatch:

jobs:
  build-and-package:
    runs-on: ubuntu-22.04

    steps:
      - name: سحب الشفرة المصدرية للنواة (Checkout Repository)
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: تثبيت أدوات البناء و GCC 10 (لتوافق نواة 4.19 القديمة)
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential ccache curl flex \
            g++-multilib gcc-multilib git gnupg gperf imagemagick lib32ncurses5-dev \
            lib32z1-dev libelf-dev liblz4-tool libncurses5-dev libssl-dev \
            libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools \
            xsltproc yasm zlib1g-dev python3 make wget zip \
            gcc-10 g++-10

      - name: إصلاح توافق Bison الحديث مع نواة 4.19
        run: |
          sed -i 's/-Werror//g' scripts/dtc/Makefile

      - name: تحميل مترجم الأكواد (Toolchain GCC 4.9)
        run: |
          mkdir -p toolchains
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9.git toolchains/aarch64-linux-android-4.9
          git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_arm_arm-linux-androideabi-4.9.git toolchains/arm-linux-androideabi-4.9

      - name: استنساخ أداة AnyKernel3 المخصصة لـ A04
        run: |
          git clone --depth=1 https://github.com/DaniTheSillyFem/AnyKernel3-a04.git AnyKernel3

      - name: حقن وتثبيت Wild_KSU داخل سورس النواة
        run: |
          echo "==> جاري سحب وتثبيت Wild_KSU باستخدام الوسيط (dev)..."
          curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s dev

      - name: إصلاح توافق Wild_KSU (file_wrapper.c) مع نواة 4.19 القديمة
        run: |
          python3 -c '
          path = "drivers/kernelsu/infra/file_wrapper.c"
          try:
              with open(path, "r") as f:
                  content = f.read()
              lines = content.splitlines()
              new_lines = []
              for line in lines:
                  if "remap_file_range" in line:
                      new_lines.append("// " + line + " // Patched for kernel 4.19")
                  else:
                      new_lines.append(line)
              with open(path, "w") as f:
                  f.write("\n".join(new_lines))
              print("Successfully patched file_wrapper.c for kernel 4.19")
          except Exception as e:
              print(f"Skipping patch or file not found: {e}")
          '

      - name: إعداد وتكوين النواة لـ Galaxy A04
        run: |
          export ARCH=arm64
          export SUBARCH=arm64
          mkdir -p out

          # 1. تطبيق ملف الإعدادات الخاص بـ Galaxy A04
          make O=out ARCH=arm64 rsuntk-a04_defconfig

          # 2. تفعيل خيارات KSU في ملف التكوين
          scripts/config --file out/.config --enable CONFIG_KSU
          scripts/config --file out/.config --enable CONFIG_KSU_TAMPER_SYS_PROC_NODE

      - name: بناء أدوات المساعد (Scripts) أولاً لتجنب أخطاء التوافق
        run: |
          make -j$(nproc) O=out \
            ARCH=arm64 \
            HOSTCC=gcc-10 \
            HOSTCXX=g++-10 \
            HOSTCFLAGS="-fcommon" \
            scripts

      - name: تجميع النواة بالكامل
        run: |
          export ARCH=arm64
          export SUBARCH=arm64
          export PATH=$PWD/toolchains/aarch64-linux-android-4.9/bin:$PWD/toolchains/arm-linux-androideabi-4.9/bin:$PATH

          echo "==> بدء تجميع النواة والتعريفات..."
          make -j$(nproc) O=out \
            ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-android- \
            CROSS_COMPILE_ARM32=arm-linux-androideabi- \
            HOSTCC=gcc-10 \
            HOSTCXX=g++-10 \
            HOSTCFLAGS="-fcommon"

      - name: دمج وحزم النواة داخل AnyKernel3
        run: |
          cp out/arch/arm64/boot/Image AnyKernel3/
          if [ -f out/arch/arm64/boot/dtb.img ]; then cp out/arch/arm64/boot/dtb.img AnyKernel3/; fi
          if [ -f out/arch/arm64/boot/dtbo.img ]; then cp out/arch/arm64/boot/dtbo.img AnyKernel3/; fi

          cd AnyKernel3
          zip -r9 ../Galaxy-A04-WildKSU-Kernel.zip *
          cd ..

      - name: رفع ملف الـ Zip النهائي الجاهز للتفليش
        uses: actions/upload-artifact@v4
        with:
          name: Galaxy-A04-WildKSU-Kernel
          path: Galaxy-A04-WildKSU-Kernel.zip
